import { createShooAuth, decodeIdentityClaims, type HandleCallbackOptions, type SessionCheckResult, type SessionMonitorOptions, type ShooAuthClient, type ShooAuthOptions, type ShooIdentity, type StartSignInOptions } from "@shoojs/auth";
import type { IdentityClaims, TokenResponse } from "@shoojs/types";
export type UseShooAuthOptions = ShooAuthOptions & {
    autoHandleCallback?: boolean;
    autoSessionMonitor?: boolean;
    sessionMonitorIntervalMs?: number;
};
export type SessionState = "unknown" | "active" | "login_required";
export type UseShooAuthResult = {
    identity: ShooIdentity;
    claims: IdentityClaims | null;
    sessionState: SessionState;
    loading: boolean;
    error: string | null;
    signIn: (params?: StartSignInOptions) => Promise<void>;
    handleCallback: (params?: HandleCallbackOptions) => Promise<TokenResponse | null>;
    checkSession: () => Promise<SessionCheckResult>;
    refreshIdentity: () => void;
    clearIdentity: () => void;
    authClient: ShooAuthClient | null;
};
export { createShooAuth, decodeIdentityClaims };
export type { HandleCallbackOptions, SessionCheckResult, SessionMonitorOptions, ShooAuthClient, ShooAuthOptions, ShooIdentity, StartSignInOptions, TokenResponse };
export declare function createShooConvexAuth(options: ShooAuthOptions): {
    useAuth: () => {
        isLoading: boolean;
        isAuthenticated: boolean;
        fetchAccessToken: (opts: {
            forceRefreshToken: boolean;
        }) => Promise<string | null>;
    };
    signIn: (opts?: StartSignInOptions) => Promise<void>;
    signOut: () => void;
};
export declare function useShooAuth(options?: UseShooAuthOptions): UseShooAuthResult;
