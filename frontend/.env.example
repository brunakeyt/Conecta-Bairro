# Base URL for the real backend API
PUBLIC_API_BASE_URL=http://localhost:3000

# Set to "true" to intercept all requests with MSW-generated mocks instead of hitting the real API.
# Set to "false" (or omit) to use the real backend.
PUBLIC_ENABLE_MOCK=true
